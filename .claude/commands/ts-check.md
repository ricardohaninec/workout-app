---
allowed-tools: Bash(bun run tsc --noEmit)
description: Run TypeScript type check
---

Run a TypeScript type check on the project using `bun run tsc --noEmit`.

Report all errors clearly, grouped by file, with the line number and a brief explanation of each issue.

If there are no errors, confirm the project is type-safe.
